apiVersion: batch/v1
kind: Job
metadata:
  name: smoketest-helloworld-job
spec:
  backoffLimit: 4
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: nginx
        image: bitnami/nginx
        command: ["echo", "hello world"]
